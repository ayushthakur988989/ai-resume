# Backend

Lightweight Node.js API scaffold for the AI Resume Builder.

## Run locally

```bash
npm run dev
```

The server starts at `http://localhost:5000`. Test it at `GET /api/health`.

No third-party package is required yet. Add database, authentication, and resume routes here as the backend grows.
